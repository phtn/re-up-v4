import { WebhookPageNav } from "../(components)/page-nav";
import { DashboardContainer } from "../(components)/views";

export const WebhookDashboard = () => {
  return (
    <DashboardContainer>
      <WebhookPageNav />
    </DashboardContainer>
  );
};
