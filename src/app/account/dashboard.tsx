"use client";
import { DashboardContainer } from "../(ui)/view";

/**
 * @component
 * @name AccountDashboard
 * @description - a component that displays the user's webhooks and active apps.
 */
export const AccountDashboard = () => {
  return (
    <DashboardContainer>
      <div className="h-[500px] w-full bg-void" />
    </DashboardContainer>
  );
};
